# V2 test suite (placeholder)

Mirrors the structure of `tests/think-food-ai/` (types.ts, cases/, run-tests.ts)
but for the V2 modules — intent-parser, cart-engine, order-state-engine,
response-builder — once they're implemented.

Nothing to run yet. The 1000+ case suite that exercises the current
production engine still lives at `tests/think-food-ai/` and is unaffected
by this folder.
