// V2 phase 6 — response builder.
//
// Responsibility: turn the (Intent, CartState, ConversationState) result of
// the deterministic pipeline into the actual WhatsApp-style reply text shown
// to the customer. This is the only place that produces user-facing copy —
// intent-parser/cart-engine/order-state-engine stay pure data in, data out.
//
// Not implemented yet — scaffolding only, so nothing imports this module.

export {};
