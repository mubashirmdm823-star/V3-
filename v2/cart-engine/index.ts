// V2 phase 4 — cart engine.
//
// Responsibility: deterministic, pure functions that resolve an Intent's item
// queries against v2/data/menu.json and apply CartOperations to a CartState
// (see v2/types/cart.ts). This is the only code allowed to change what's in
// the cart — the AI/intent-parser never does this directly.
//
// Not implemented yet — scaffolding only, so nothing imports this module.

export {};
